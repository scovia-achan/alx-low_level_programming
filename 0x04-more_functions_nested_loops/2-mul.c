#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first parameter
 * @b: second parameter
 * Return: always the product of the two
 */

int mul(int a, int b)
{
	int res;

	res = a * b;
	return (res);
}
